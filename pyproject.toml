[project]
name = "sltiktok"
version = "0.1.0"
description = "TikTok 7-Eleven staff-voice pipeline: discover, enrich, analyze, dashboard"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
